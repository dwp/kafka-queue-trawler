groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - kafka-queue-trawler-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-kafka-queue-trawler-dev
      - mirror-dwpdigital-kafka-queue-trawler
